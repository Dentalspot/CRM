import React from 'react';

/**
 * Filter bar for demographics stats page
 */
const DemographicsFilters = ({ onFilterChange }) => {
  return (
    <div className="flex gap-2 p-4 bg-muted/20 rounded-lg">
      <span>Filtros Demográficos (Stub)</span>
    </div>
  );
};

export default DemographicsFilters;