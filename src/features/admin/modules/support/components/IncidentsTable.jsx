import React from 'react';
import { Table } from '@/components/ui/table';

/**
 * Table for tracking active and past incidents.
 */
const IncidentsTable = ({ incidents }) => <Table>Incidents...</Table>;
export default IncidentsTable;