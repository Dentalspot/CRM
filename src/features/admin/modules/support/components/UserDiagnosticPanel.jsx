import React from 'react';

/**
 * Panel showing user's system diagnostics (device, browser, last IP).
 */
const UserDiagnosticPanel = ({ data }) => <div className="border p-4">Diagnostics</div>;
export default UserDiagnosticPanel;