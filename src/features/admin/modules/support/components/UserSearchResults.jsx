import React from 'react';

/**
 * Component to display user search results.
 */
const UserSearchResults = ({ results }) => <div>Results Found: {results?.length}</div>;
export default UserSearchResults;