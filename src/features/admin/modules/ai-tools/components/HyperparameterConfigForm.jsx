import React from 'react';

/**
 * Form to configure training hyperparameters.
 */
const HyperparameterConfigForm = () => (
  <div className="space-y-4">
    {/* Inputs for learning rate, epochs, etc. */}
    <p>Configuración de Hiperparámetros</p>
  </div>
);

export default HyperparameterConfigForm;