## Summary

- Spec 028: asignación obligatoria de dentista responsable a cada cita. Driver: Odontología Los Álamos sumó al Dr. Pablo Ceballos (ortodoncia) junto a Cristobal Tagle, ambos atendiendo en box 1, sin forma formal de declarar quién atiende qué cita
- Reusa columna existente `appointments.therapist_id` (legacy naming Communicare/FONOKIT — semánticamente "dentista responsable"). Sin nueva columna, sin backfill (NOT NULL desde baseline)
- Migration única `20260529000001`: trigger valida dentista activo de la misma org + `WITH CHECK` en RLS `appt_dentist_update` + ampliación de CHECK constraints en `clinical_audit_log` (action: `view`/`create`/`update`/`cancel`/`appointment_reassigned`; resource_type: `appointment`). Cierra incidentalmente bug pre-existente del módulo asistente donde audit logs caían silenciosamente
- Frontend: selector "Dentista *" obligatorio en `AppointmentModal` + `AssistantAppointmentModal` con default smart por rol (dentista=self, admin/asistente=vacío). Card teal "Ubicación" en `OrgCalendarView` con dropdown "Todos los dentistas" persistido en URL `?dentist=uuid` y cross-org guard silencioso. `border-l-4` + footer "Dr. {apellido}" por dentista en chips del calendario. Hook nuevo `useUserRoleInOrg`

**Out of scope** (specs futuras): booking online paciente, `clinic_box_dentists`, push notifications, rename `therapist_id` → `dentist_id` (Founder Opción A: mantener legacy)

## Test plan

- [ ] Aplicar migration en prod: `npx supabase db push`
- [ ] Invitar a Pablo Ceballos a Odontología Los Álamos (pre-req para smoke §9)
- [ ] §3 Asistente crea cita con selector obligatorio → audit log con `action='create'`
- [ ] §4 Filtro URL `?dentist=uuid` persiste tras refresh; uuid de otra org se resetea silencioso
- [ ] §5 Login como Cristobal (admin+dentista) → modal pre-selecciona "Dr. Cristobal Tagle"
- [ ] §6 Reasignar cita admin → audit log con `action='appointment_reassigned'` y `reason='from:X;to:Y'`
- [ ] §7 Dentista NO puede UPDATE cita ajena (RLS bloquea)
- [ ] §8 Trigger DB bloquea INSERT cross-org con `dentist_not_active_in_org`
- [ ] §9 Visual: chips de cada dentista con `border-l-4` de color distinto + footer "Dr. Apellido"
- [ ] Regresión: asistente edita cita propia (sin reasignar) → sigue funcionando
- [ ] Regresión: dentista cancela cita propia → audit log con `action='cancel'`

Spec completa: `specs/028-appointment-dentist-assignment/`
Quickstart con smokes detallados: `specs/028-appointment-dentist-assignment/quickstart.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
